/*
function toggleInfo() {
    const actionType = document.querySelector('input[name="actionType"]:checked').value;
    const infoSection = document.getElementById('infoSection');

    if (actionType === "tra-hang") {
        // Hiển thị các thông tin khi chọn "Trả Hàng"
        infoSection.style.display = "block";
    } else {
        // Ẩn các thông tin khi chọn "Hoàn Trả"
        infoSection.style.display = "none";
    }
}

// Gọi hàm để kiểm tra trạng thái ban đầu khi trang được tải
document.addEventListener('DOMContentLoaded', toggleInfo);
*/
